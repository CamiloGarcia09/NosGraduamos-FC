spring.config.activate.on-profile=azure
logging.config=classpath:logback-azure.xml

# Azure uses the VM's managed identity through DefaultAzureCredential.
spring.cloud.azure.credential.managed-identity-enabled=true
